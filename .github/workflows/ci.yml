name: CI - Integración Continua

on:
  pull_request:
    branches: [develop]
  push:
    branches: [develop, feature/*]

jobs:
  # ============================================
  # Backends en paralelo
  # ============================================
  backend-checks:
    strategy:
      matrix:
        microservice: [back-Despachos_SpringBoot, back-Ventas_SpringBoot]
    runs-on: ubuntu-latest
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4

      - name: ☕ Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: 🧪 Compilar y empaquetar
        run: |
          cd ${{ matrix.microservice }}
          mvn clean package -DskipTests -B
        shell: bash

  # ============================================
  # Frontend
  # ============================================
  frontend-checks:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: front_despacho
    steps:
      - name: 🔄 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Instalar dependencias
        run: npm install || echo "Sin dependencias npm"

      - name: 🏗️ Build
        run: npm run build 2>/dev/null || echo "Proyecto vanilla JS - No requiere build"
